# Express
PORT=5000
NODE_ENV=development

# Local Postgres (outside Docker or with port 5432 published to host)
# If you run only DB in Docker locally, use 127.0.0.1 as host.
PGURI=postgres://postgres:postgres@127.0.0.1:5432/appdb

# JWT + CORS
JWT_SECRET=ersätt mig med en hemlig kod
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
